import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const userManagementReducer = createSlice({
  name: "userManagementReducer",
  initialState,
  reducers: {}
});

export const {} = userManagementReducer.actions

export default userManagementReducer.reducer